import type { PasteResource } from "../types";

export type {
  AppBootstrap,
  ErrorCode,
  ExpirationInput,
  HistoryList,
  MutationResult,
  PasteSummary,
  RevisionResource,
} from "../types";

export type RemoteSnapshot = PasteResource;
